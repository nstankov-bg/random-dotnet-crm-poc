apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: random-dotnet-crm-poc-default
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/random-dotnet-crm-poc-default: "true"
  podSelector:
    matchLabels:
      io.kompose.network/random-dotnet-crm-poc-default: "true"
